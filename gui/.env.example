# Copy to gui/.env and fill in values

# Backend API base URL — leave empty to use Vite's proxy (localhost:8000)
VITE_API_URL=

# LiveKit WebSocket URL — browser connects DIRECTLY to LiveKit, not through FastAPI
# Use ws:// for local dev, wss:// for production
VITE_LIVEKIT_URL=ws://localhost:7880
