# Copy to .env and fill in values

# Anthropic (Safety Agent summary when LLM_PROVIDER=claude)
ANTHROPIC_API_KEY=

# LiveKit — must match keys defined in livekit.yaml
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=devsecret
LIVEKIT_HOST=http://localhost:7880
# For phone demo (pick one):
#   LAN (same Wi-Fi):  ws://YOUR_LAN_IP:7880
#   ngrok (any network): wss://YOUR_LIVEKIT_NGROK_URL  (from ngrok.yml livekit tunnel)
# LIVEKIT_PUBLIC_WS_URL=ws://192.168.1.100:7880
# LIVEKIT_PUBLIC_WS_URL=wss://def456.ngrok-free.app

# LLM (Safety Agent executive summary — default: local Ollama)
LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.2
# Or use Claude: LLM_PROVIDER=claude (and set ANTHROPIC_API_KEY above)

# Supabase (optional — app falls back to in-memory storage when unset)
SUPABASE_URL=
SUPABASE_KEY=

# Vast.ai GPU instance (Video Agent — optional)
VASTAI_API_KEY=
VASTAI_BASE_URL=
VASTAI_INSTANCE_ID=
VASTAI_MODEL_ID=llava-hf/LLaVA-NeXT-Video-34B-hf

# NVIDIA Parakeet (STT)
NVIDIA_PARAKEET_API_KEY=
NVIDIA_PARAKEET_GRPC_SERVER=grpc.nvcf.nvidia.com:443
NVIDIA_PARAKEET_FUNCTION_ID=d8dd4e9b-fbf5-4fb0-9dba-8cf436c8d965

# NVIDIA NIM API key (Grounding DINO detection)
NVIDIA_API_KEY=

# NV-CLIP embeddings (optional; falls back to NVIDIA_API_KEY if not set)
NVIDIA_EMBED_API_KEY=
